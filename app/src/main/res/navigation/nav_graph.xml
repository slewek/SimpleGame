<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:id="@+id/nav_graph"
    app:startDestination="@id/menuActivity">

    <activity
        android:id="@+id/menuActivity"
        android:name="com.example.simplegame.MenuActivity"
        android:label="MenuActivity" >
        <action
            android:id="@+id/action_menuActivity_to_mainActivity"
            app:destination="@id/mainActivity" />
    </activity>
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.simplegame.MainActivity"
        android:label="MainActivity" />
</navigation>